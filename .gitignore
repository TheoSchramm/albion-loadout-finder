# Python bytecode
__pycache__/
*.py[cod]

# Build output - frontend/dist is a plain copy of frontend/src, regenerated by
# `node scripts/build.mjs` and by CI before deploying. Committing it means a stale
# copy can silently shadow src (backend/main.py prefers dist when it exists).
frontend/dist/
